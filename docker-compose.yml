version: '3.8'
services:
  app-thru:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    volumes:
      - app-data:/app/backend
    restart: unless-stopped

volumes:
  app-data:
